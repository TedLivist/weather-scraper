# rubocop: Layout/EndOfLine, Lint/RedundantCopDisableDirective

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'byebug'
gem 'httparty'
gem 'nokogiri'

gem 'rubocop', '>= 1.0', '< 2.0'
